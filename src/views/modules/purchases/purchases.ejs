<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1>
          {{%= __('Purchases') %}}
          <small class="text-muted">{{%= __('List') %}}</small>
        </h1>
      </div>
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item">
            <a href="/">{{%= __('Dashboard') %}}</a>
          </li>
          <li class="breadcrumb-item active text-muted">
            {{%= __('Purchases') %}}
          </li>
        </ol>
      </div>
    </div>
  </div>
</section>

<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-8 col-sm-4 col-md-3">
        <div class="info-box">
          <span class="info-box-icon bg-primary elevation-1"
            ><i class="fas fa-store"></i
          ></span>
          <div class="info-box-content">
            <span class="info-box-text">{{%= __('Purchases') %}}</span>
            <span class="info-box-number">
              {{%= purchases.length %}}
            </span>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-md-12">
        <div class="card card-primary card-outline">
          <div class="card-header">
            <h3 class="card-title">
              <i class="fas fa-store"></i>
              {{%= __('Purchases') %}}
            </h3>
            <div class="card-tools">
              <a
                href="/dashboard/purchases/new"
                class="btn btn-success"
                title="{{%= __('New Purchase') %}}"
              >
                <i class="fas fa-plus"></i> &nbsp; {{%= __('New Purchase') %}}
              </a>
            </div>
          </div>
          <div class="card-body">
            <table
              class="
                table table-bordered table-striped
                dataTable
                dataTableConfig
                dtr-inline
              "
              aria-describedby="dataTableConfig_info"
            >
              <thead>
                <tr>
                  <th>#</th>
                  <td>{{%= __('Reference Code') %}}</td>
                  <td>{{%= __('Creation Date') %}}</td>
                  <td>{{%= __('Details') %}}</td>
                  <th>{{%= __('Actions') %}}</th>
                </tr>
              </thead>
              <tbody>
                {{% purchases.forEach((row, i) => { %}}
                <tr>
                  <td>{{%= i + 1 %}}</td>
                  <td>
                    <a
                      href="/dashboard/purchases/{{%= row.id_purchase %}}"
                      >{{%= row.reference_code %}}</a
                    >
                  </td>
                  <td>{{%= row.creation_date %}}</td>
                  <td>{{%= row.details %}}</td>
                  <td>
                    <div class="column-actions">
                      <a
                        href="#"
                        data-toggle="modal"
                        data-target="#modalDeletePurchase"
                        data-id="{{%= row.id_purchase %}}"
                        class="btn btn-danger btn-xs"
                      >
                        <i class="fas fa-trash-alt"></i>
                      </a>
                      <!-- <a
                        href="#"
                        data-toggle="modal"
                        data-target="#modalEditPurchase"
                        data-row="{{%# JSON.stringify(row) %}}"
                        data-id="{{%# row.id_purchase %}}"
                        class="btn btn-warning btn-xs"
                      >
                        <i class="fas fa-pen"></i>
                      </a> -->
                    </div>
                  </td>
                </tr>
                {{% }) %}}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{{%- include('../../components/ModalDelete', { idModal: "modalDeletePurchase", route:['/dashboard/purchases/','/delete'], title: "Purchase" }) %}}
